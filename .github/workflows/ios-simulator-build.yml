name: Build iOS Simulator App

on:
  workflow_dispatch:
    inputs:
      branch:
        description: 'Branch to build'
        required: true
        default: 'main'

jobs:
  build-simulator:
    name: Build iOS Simulator App
    runs-on: macos-latest
    
    steps:
      - name: Checkout code
        uses: actions/checkout@v3
        with:
          ref: ${{ github.event.inputs.branch }}
      
      - name: Setup Node.js
        uses: actions/setup-node@v3
        with:
          node-version: '18'
          cache: 'npm'
      
      - name: Install dependencies
        run: npm ci
      
      - name: Clean iOS build (force fresh build)
        run: |
          echo "🧹 Cleaning iOS build cache and old files..."
          rm -rf ios/App/build
          rm -rf ios/App/App.xcworkspace/xcuserdata
          rm -rf ios/App/App.xcodeproj/xcuserdata
          rm -rf ~/Library/Developer/Xcode/DerivedData/*
          # CRITICAL: Remove old public folder to force fresh copy
          rm -rf ios/App/App/public
          echo "✅ iOS cache and old files cleaned!"
      
      - name: Sync Capacitor
        run: npx cap sync ios
      
      - name: Copy App Icons (AFTER sync to prevent overwrite)
        run: |
          echo "📱 Copying app icons to iOS project..."
          if [ -d "android/AppIcons/Assets.xcassets/AppIcon.appiconset/_" ]; then
            # Remove default Capacitor icons
            rm -rf ios/App/App/Assets.xcassets/AppIcon.appiconset/*
            # Copy all icon files including Contents.json
            cp -r android/AppIcons/Assets.xcassets/AppIcon.appiconset/* ios/App/App/Assets.xcassets/AppIcon.appiconset/
            echo "✅ App icons copied successfully!"
            echo "📋 Icon files:"
            ls -la ios/App/App/Assets.xcassets/AppIcon.appiconset/
          else
            echo "❌ ERROR: App icons not found at android/AppIcons/Assets.xcassets/AppIcon.appiconset/_"
            exit 1
          fi
      
      - name: Build for Simulator
        run: |
          cd ios/App
          xcodebuild \
            -workspace App.xcworkspace \
            -scheme App \
            -configuration Debug \
            -sdk iphonesimulator \
            -derivedDataPath build \
            CODE_SIGNING_ALLOWED=NO \
            CODE_SIGNING_REQUIRED=NO \
            CODE_SIGN_IDENTITY="" \
            CODE_SIGN_ENTITLEMENTS="" \
            DEVELOPMENT_TEAM=""
      
      - name: Package Simulator App
        run: |
          cd ios/App/build/Build/Products/Debug-iphonesimulator
          zip -r App.app.zip App.app
          mv App.app.zip $GITHUB_WORKSPACE/
      
      - name: Upload Simulator App
        uses: actions/upload-artifact@v4
        with:
          name: ios-simulator-app
          path: App.app.zip
          retention-days: 7
      
      - name: Build Summary
        run: |
          echo "✅ iOS Simulator app built successfully!"
          echo ""
          echo "📦 Download App.app.zip from Artifacts"
          echo "🌐 Upload to https://appetize.io/upload to test"
          echo ""
          echo "📱 Includes:"
          echo "   ✅ LEO SUSHI app icons"
          echo "   ✅ Cart button fix"
          echo "   ✅ Mobile app CSS fixes"
          echo "   ✅ All latest code"
